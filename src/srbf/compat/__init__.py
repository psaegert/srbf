"""Third-party model-adapter glue for srbf (e.g. NeSymReS).

The flash-ansr ``compat`` package re-exported ``ParserFactory`` (a flash-ansr core symbol); that
alias stays in flash-ansr. srbf's ``compat`` carries only the third-party adapter helpers.
"""
